# 教學指引（給「教學 agent」用的說明書）

> 這份檔案是要交給「另一個 AI agent」當教學指引用的。
> 目的：把 GeoFlip 這個專案**教到學生能對教授逐行解釋每一段 code**。
> 學生會把這份檔案連同整個 repo 給你（教學 agent）。請依此進行。

---

## 1. 學生是誰（很重要，先讀）

- 清大資工系大一下，修「I2P_Python」。
- **課表上教過很多**（class、`__init__`、`@property`、`@classmethod`、decorator、lambda、
  list comprehension、`with open`、json、`try/except`、generator、requests、flask、folium、
  math/functools/itertools），**但他幾乎沒去上課**，所以這些大多「聽過名字、不會用、講不出來」。
- 他自評真正熟的只有：`def`、`import`、`if/elif/else`、`for/while`、`list`/`dict` 操作。`lambda` 他說他會。
- **最終目標：教授會逐行拷問，他必須能用自己的話講出每一段在幹嘛**。所以「看得懂」不夠，要「講得出來」。

## 2. 教學原則

1. **從零教，不要假設他懂 class。** 尤其 OOP，先講「為什麼需要」再講語法。
2. **講概念 → 指著專案裡真正的 code → 讓他用自己的話複述 → 隨機抽問。** 不要只丟解釋。
3. **每教完一塊，做一次「教授模擬拷問」**：你扮教授指一行問「這在幹嘛？為什麼這樣寫？」，他答，你補。
4. 用**白話＋具體例子**，少用術語；非講不可的術語（物件、方法、實例…）要當場解釋。
5. 寧可慢、寧可重複。確認他「能複述」再往下一塊。

## 3. 建議教學順序

> repo 裡已經有一份逐檔講解 **`程式講解.md`**（含「第 1 節：從零教 OOP」與「第 5 節：教授問答」）。
> 請以它為教材骨幹，你的工作是把它「講出來、考出來」，而不是重寫。

1. **OOP 基礎**（最關鍵）。用最小的 `class Poi`（`app/models.py`）當例子，逐行帶：
   - class＝藍圖、物件＝蓋出來的實體；`a = Poi(...)`。
   - `__init__`＝建立物件時自動跑；`self`＝「物件自己」；`self.name = name` 怎麼讀。
   - 方法＝class 裡的函式（第一個參數 `self`），呼叫 `state.scores()` 時 self 自動帶入。
   - `@classmethod`（`GameState.new_game`、`Poi.from_dict`）＝用整個類別呼叫的「工廠」。
   - decorator＝函式上面加 `@xxx`，效果是「加工/登記」下面的函式（`@classmethod`、`@app.route`）。
   - **檢核**：要他不看稿，用自己的話解釋 `Poi` 的 `__init__` 每一行、以及 `state.get_poi("x")` 怎麼運作。
2. **資料模型** `app/models.py`：`Poi`、`GameState`、`score_poi`、`mmss`。重點：`scores()` 為何「即時算」。
3. **存檔** `app/state.py`：`class StateStore`、`with open`、`json.load/dump`、`to_dict`/`from_dict` 來回。
4. **服務層**：
   - `app/services/osrm.py`（`OsrmClient`，requests，回傳字典；600 秒規則靠 `duration_s`）。
   - `app/services/nominatim.py`（`NominatimClient`，搜地點；403 是 User-Agent 問題）。
   - `app/services/overpass.py`（`OverpassClient`，抓棋盤；分類表、間距過濾）。
   - `app/services/geometry.py`（**Shapely + pyproj**；先轉公尺再 `.buffer(50)` 再 `.covers()`）。
5. **規則引擎** `app/game/rules.py`（**最重要、教授最愛問，花最多時間**）：
   - `RulesEngine.apply_move` 的檢查順序、開局 vs 正常回合。
   - `_commit_normal` 的 buffer 翻面規則（阻斷規則、為何 deepcopy）。
   - 為什麼 `osrm` 是「當參數傳進來」（→ 測試可傳假的；見 `check_rules.py` 的 `FakeOsrm`）。
6. **地圖** `app/map/render.py`（Folium；座標要對調；popup 不能放 `<script>`）。
7. **網站** `app/web.py`（Flask；`@app.route` 把網址綁到函式；讀→算→寫→轉址）。
8. **前端** `templates/*.html`（Jinja `{{ }}`/`{% %}`，把 Python 值填進網頁）。

## 4. 一定要讓他能解釋的「重點/陷阱」

- **無效的一手是完全 no-op**：先檢查、全過才 `deepcopy` 再改 → 為什麼這樣能保證不留髒資料。
- **buffer 為何要先轉公尺**：經緯度的「度」不能直接量公尺。
- **阻斷規則**：走廊內中立 POI 達到門檻 `GAME_BLOCK_NEUTRAL_COUNT`（預設 2）→ 只翻目標；不到門檻 → 翻走廊內所有對手；自己的點不擋不翻。（門檻是 config 旋鈕：1=最難翻、越大越容易翻＝越有拉扯感。）
- **座標順序**：Folium `[緯度,經度]`、OSRM `[經度,緯度]`，畫線前要對調。
- **popup 不能含 `<script>`**：會把 Folium 外層 script 提前關掉。
- **`scores()` 即時算**：不存舊分，翻面後分數自然對。
- **Nominatim 403**：User-Agent 要可識別+真實信箱（`app/config.py`）。
- **OOP 的好處**（用 `apply_move(..., osrm)` 說明）：正式傳真 client、測試傳假 client，引擎不用改。

## 5. 學生學完應該答得出的「驗收問題」

讓他能流暢回答（其餘見 `程式講解.md` 第 5 節）：
1. 你哪裡用到 class / OOP？各個 class 負責什麼？
2. `self` 是什麼？`__init__` 什麼時候執行？
3. 整場遊戲的資料怎麼存、怎麼讀？（GameState ↔ 字典 ↔ JSON）
4. 翻面規則完整講一遍；阻斷規則為什麼存在？
5. 50 公尺 buffer 怎麼算？為什麼要轉座標？（Shapely/pyproj）
6. 無效的一手為什麼不會改到狀態？
7. 步行時間哪來的？600 秒規則在哪判斷？
8. Nominatim 和 Overpass 差在哪？
9. Flask 的 `@app.route` 在做什麼？一次 `/move` 請求完整流程？

## 6. repo 內可用資源

- `程式講解.md` — 逐檔講解 + 從零教 OOP + 教授問答（**主要教材**）。
- `check_rules.py` — 12 項規則檢查，可邊跑邊講「這在驗哪條規則」。
- `CLAUDE.md` — 架構、規則、座標慣例（給你 agent 快速掌握全貌）。
- 跑起來：`pip install -e .` → `python -m app.web` → 開 http://127.0.0.1:5000 ；`python check_rules.py`。

## 7. 邊界

- **不要把專案改成超綱寫法**（dataclass / Protocol / 依賴注入 / type hint / httpx）——維持現在的「課堂程度 OOP」。
- 教學重點是「讓他講得出來」，不是把 code 改得更炫。
