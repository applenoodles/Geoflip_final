# GeoFlip 程式逐檔講解（class 版）

> 這份文件幫你「看懂每一行、能跟教授解釋」。
> 你的課教過 class、`__init__`、`@classmethod`、decorator、lambda、list comprehension、
> `with open`、JSON、`try/except`、requests、flask、folium——這份專案就是用這些寫的。
> 第 1 節先**從零把 OOP（class）講清楚**，因為你說沒上到課；後面再逐檔講解。

---

## 0. 這個遊戲在做什麼（30 秒版）

兩個人輪流在「真實地圖」上搶地盤：

1. 開局先在地圖上點幾個地點佈子（插旗）。
2. 之後每一回合：先點自己的一個地點當「起點」，再點一個中立地點當「目標」，
   系統用 **真實步行路線** 把兩點連起來。
3. 目標一定變成你的；如果路線兩側 50 公尺內「中立點不夠多、擋不住路」（預設要 2 個才擋），沿途的對手地點也會被你翻過來。
4. 回合用完、或地點被佔光、或連續兩次跳過，就結束，分數高的贏。

---

## 1. 先搞懂 OOP / class（從零開始）

### 1-1. class 想解決什麼問題？

假設「一個地點」有名字、座標、分數、屬於誰。如果不用 class，你可能會這樣存：

```python
poi = {"name": "新竹車站", "lat": 24.8, "lon": 120.97, "owner": None}
```

這樣可以，但「操作這份資料的函式」會散落各地，例如要另外寫 `def get_score(poi): ...`。
**class 的核心想法就是：把『資料』和『操作這份資料的函式』綁在一起**，變成一個「東西（物件）」。

### 1-2. class 與物件（藍圖 vs 蓋出來的房子）

- **class（類別）** = 藍圖／模具，描述「這種東西長什麼樣、會做什麼」。
- **object（物件）** = 用藍圖蓋出來的實體。

```python
class Poi:          # 這是藍圖
    ...

a = Poi(...)        # a 是用藍圖蓋出來的一個實體（物件）
b = Poi(...)        # b 是另一個，跟 a 各自獨立
```

### 1-3. `__init__` 與 `self`（最重要）

```python
class Poi:
    def __init__(self, name, lat, lon):
        self.name = name      # 把傳進來的 name，記在「這個物件自己」身上
        self.lat = lat
        self.lon = lon
```

- `__init__` 叫**建構子**：每次 `Poi(...)` 建立一個新物件時，Python **自動**呼叫它，負責把欄位裝好。
- `self` 就是**「物件自己」**。`self.name = name` 的意思是「把參數 name 存到這個物件的 name 欄位」。
- 之後就能 `a = Poi("新竹車站", 24.8, 120.97)`，再用 `a.name` 拿到 `"新竹車站"`。

> 小抄：看到 `self.x`，就讀成「這個物件的 x」。

### 1-4. 方法（method）= class 裡的函式

```python
class GameState:
    def scores(self):                 # 這是一個「方法」
        result = {1: 0, 2: 0}
        for poi in self.pois:         # self.pois = 這個 state 的所有 POI
            ...
        return result
```

- class 裡定義的函式叫**方法**，第一個參數固定是 `self`。
- 呼叫時**不用自己傳 self**：寫 `state.scores()`，Python 會自動把 `state` 當成 `self` 塞進去。
- 方法可以直接用 `self.xxx` 拿到這個物件的資料——這就是「資料和操作綁在一起」。

### 1-5. `@classmethod`（用整個類別呼叫的方法）

```python
class GameState:
    @classmethod
    def new_game(cls, max_turns=20):
        return cls(...)               # cls 就是 GameState 這個類別本身
```

- 一般方法是「用某個物件呼叫」(`state.scores()`)；`@classmethod` 是「**用整個類別呼叫**」(`GameState.new_game()`)。
- 第一個參數是 `cls`（代表類別本身），`cls(...)` 等於 `GameState(...)`，回傳一個**新做好的物件**。
- 我們用它來當「工廠」：`GameState.new_game()` 做一場新遊戲、`Poi.from_dict(d)` 從字典做回一個 Poi。

### 1-6. decorator（裝飾器）= 在函式上面加一行 `@xxx`

`@classmethod`、`@app.route("/")` 這種「加在函式上面的 `@xxx`」就叫 **decorator**。
你只要記得它的效果：**它會「加工」或「登記」下面那個函式**。

- `@classmethod`：把下面的函式變成「用類別呼叫」的方法。
- `@app.route("/move")`：把下面的函式「登記」成「有人連到 /move 時要執行的處理器」。

你不用自己寫 decorator，只要會「用」它（在函式上面加那一行）就好。

> 課堂也教過 `@property`（把方法包裝成「像欄位一樣用」）。這個專案剛好用不到，
> 但如果教授問，你可以說：「`@property` 能讓 `obj.x` 看起來像取欄位、實際上是執行一個方法。」

### 1-7. 這個專案有哪些 class？

| class | 在哪 | 角色 |
|---|---|---|
| `Poi` | `app/models.py` | 一個地點（名字/座標/分數/屬於誰）+ 轉字典的方法 |
| `GameState` | `app/models.py` | 整場遊戲狀態 + 一堆查詢/計分方法 |
| `StateStore` | `app/state.py` | 包住「讀檔/存檔/刪檔」 |
| `OsrmClient` | `app/services/osrm.py` | 包住「跟 OSRM 要路線」 |
| `NominatimClient` | `app/services/nominatim.py` | 包住「跟 Nominatim 搜地點」 |
| `OverpassClient` | `app/services/overpass.py` | 包住「跟 Overpass 抓一堆 POI」 |
| `RulesEngine` | `app/game/rules.py` | 遊戲裁判：判斷合法、翻面、結束 |

它們全都是**同一個模式**：`__init__` 把資料裝好 → 提供幾個方法操作。看懂一個，其他都一樣。

---

## 2. 整體架構：你點一下，背後發生什麼事

這是一個「網站」程式。瀏覽器是畫面，Python 在後面算規則。**一次「連線到此」的點擊**會這樣跑：

```
瀏覽器送出表單  POST /move（夾帶 目標id、起點id）
   │
   ▼
app/web.py        ← Flask 收到，呼叫 engine.apply_move(...)
   │
   ▼
app/game/rules.py ← RulesEngine 判斷合不合法、算要翻誰（中間呼叫 osrm.route(...)）
   │
   ▼
app/state.py      ← store.save(...) 把新的 GameState 存回 data/state.json
   │
   ▼
轉址回首頁 GET /  →  web.py 再 store.load() 一次，重新畫畫面
                      地圖由 app/map/render.py 用 Folium 畫出來
```

**關鍵設計：程式本身不「記住」任何東西。** 每次請求都是「讀檔 → 算 → 寫檔」。
所以重新整理瀏覽器也不會出錯——真相永遠在 `data/state.json` 這個檔案裡。

---

## 3. 五個外部服務／套件，各是什麼？（作業要求的重點）

| 名稱 | 是什麼 | 在這專案做什麼 | 在哪 |
|---|---|---|---|
| **Flask** | 做網站的套件 | 接住瀏覽器請求、回傳網頁 | `app/web.py` |
| **Nominatim** | 「地名 → 座標」搜尋服務 | 開局搜尋起始地點 | `app/services/nominatim.py` |
| **Overpass** | 「抓某區域一堆地點」服務 | 抓中心點附近 18~36 個 POI 組棋盤 | `app/services/overpass.py` |
| **OSRM** | 「算路線」服務 | 算兩點步行路線與時間（600 秒規則） | `app/services/osrm.py` |
| **Folium** | 用 Python 畫互動地圖 | 把 POI、路線畫成可點的網頁地圖 | `app/map/render.py` |
| **Shapely + pyproj** | 幾何/座標計算 | 把路線加寬成 50m 走廊、判斷點在不在裡面 | `app/services/geometry.py` |

> 連網的部分用 **requests**：把參數組成網址 → `requests.get(...)` / `requests.post(...)` → 它回一包 JSON（巢狀字典/串列）→ 讀出需要的欄位。

---

## 4. 逐檔講解

### 4-1. `app/config.py` — 所有設定值

一堆**普通常數**（`GAME_MAX_WALK_SECONDS = 600` 等）。想改遊戲行為就改這裡。
其中 `GAME_BLOCK_NEUTRAL_COUNT`（預設 2）是「拉扯感旋鈕」：走廊內要幾個中立點才擋住路線，調小最難翻、調大最容易翻。
裡面也放了 Nominatim 的 `User-Agent` / `EMAIL`（**必須是真實可識別的，否則 Nominatim 回 403**）。

### 4-2. `app/models.py` — `Poi` 與 `GameState` 兩個 class

- 上面兩個普通函式：`score_poi(...)`（依種類給分）、`mmss(...)`（秒數拆成分:秒）。
- **`Poi`**：`__init__` 裝好一個地點的所有欄位。
  - `Poi.from_api(...)`（`@classmethod`）：從 API 抓到的資料做一個中立 Poi，分數自動算好。
  - `to_dict()` / `from_dict()`：Poi 物件 ↔ 字典（存/讀 JSON 時用）。
- **`GameState`**：整場遊戲。`__init__` 裝好所有欄位（`pois` 是一串 Poi 物件、`routes`/`moves` 是字典串列）。
  - `GameState.new_game()`（`@classmethod`）：開新局。
  - 方法：`current_player_id()`（`turn_index % 2`）、`get_poi(id)`、`neutral_pois()`、`owned_pois(pid)`、
    `scores()`（即時加總，**對應規則「分數不存舊值」**）、`winner()`、`is_finished()`、`in_opening_phase(...)`、
    `last_two_moves_are_passes()`、`merge_discovered_pois(...)`、`to_dict()` / `from_dict()`。

### 4-3. `app/state.py` — `StateStore` class（存檔）

`__init__(path)` 記住檔案路徑；三個方法：

```python
def load(self):
    with open(self.path, "r", encoding="utf-8") as f:  # 開檔，用完自動關
        data = json.load(f)                            # JSON 文字 → 字典
    return GameState.from_dict(data)                   # 字典 → GameState 物件

def save(self, state):
    with open(self.path, "w", encoding="utf-8") as f:
        json.dump(state.to_dict(), f, ensure_ascii=False, indent=2)
```

- `with open(...) as f:` = 開檔、用完自動關。
- `json.load` / `json.dump` = 字典和「JSON 文字」互轉。
- `save` 接到的是 GameState **物件**，先 `.to_dict()` 變字典再寫檔。

### 4-4. `app/services/geometry.py` — Shapely + pyproj（作業重點）

**問題：** 經緯度的「度」不能直接量公尺。所以：

```
經緯度 ──(pyproj 換算)──► 公尺座標 ──(shapely 加寬/判斷)──► 答案
```

這檔是**普通函式**（座標計算不需要 class）：

- `build_meter_transformers(...)`：做兩個轉換器（經緯度↔公尺）。`always_xy=True` 固定要加。
- `route_to_meter_linestring(...)`：把路線點換成公尺、組成一條線 `LineString`。
- `buffer_route_meters(line, 50)`：`line.buffer(50)` 把線加寬 50 公尺成走廊多邊形。← **這就是「50 公尺 buffer」。**
- `point_in_buffer(...)`：`polygon.covers(Point(x, y))` 判斷某點在不在走廊裡。

### 4-5. `app/services/osrm.py` — `OsrmClient` class

`__init__` 記住伺服器網址；`route(from_lat, from_lon, to_lat, to_lon)` 方法：
組網址（OSRM 規定「經度,緯度」）→ `requests.get(...)` → 讀出路線座標、距離、秒數，打包成字典回傳。
失敗就 `raise Exception`，呼叫它的規則引擎會把那手當無效。**600 秒規則靠它回傳的 `duration_s`。**

### 4-6. `app/services/nominatim.py` — `NominatimClient` class

`__init__` 記住網址/User-Agent/信箱；`search_locations(query)` 方法：
組網址 → `requests.get(...)` → 用 for 迴圈把結果整理成候選清單（字典），順手去重複、把台灣排前面（用兩個 list comprehension 分裝再相加）。

### 4-7. `app/services/overpass.py` — `OverpassClient` class

`__init__` 記住網址/最小間距；`fetch_board_pois(...)` 方法：
`_build_query(...)` 組查詢字串 → `requests.post(...)` → for 迴圈用 `_normalize(...)` 把每筆變成 `Poi` 物件（去重複）→ `sorted(..., key=lambda poi: (-poi.score, poi.id))` 分數高的排前面 → `_spread_filter(...)` 把太近的點去掉 → 最多留 `limit` 個。
（`_pick_category`/`_pick_name`/`_CN_TYPE_NAMES` 是判斷種類、補中文名字的小工具。）

### 4-8. `app/game/rules.py` — `RulesEngine` class（整個專案的心臟）

**教授最可能拷問這支。** `__init__` 記住三個規則參數（600 秒、50 公尺、開局 2 子）。

**兩個入口方法：**
- `apply_move(state, target_id, osrm, source_id=None)`：玩家落子。
- `apply_pass(state)`：跳過一回合。

注意 `apply_move` 的參數 `osrm`：呼叫者把「會 `.route()` 的物件」傳進來，引擎只管呼叫它。
正式跑傳真的 `OsrmClient`，測試傳假的 `FakeOsrm`（見 `check_rules.py`）——引擎完全不用改。**這就是 OOP 的好處之一。**

**`apply_move` 流程（對照規則念）：**
1. 先檢查（**這階段絕不改 state**）：遊戲結束？目標存在？目標是中立？
2. 還在**開局** → 不準帶起點 → `_commit_opening`（只插旗、回合+1）。
3. **正常回合** → 必須有起點，且起點是自己的、不能跟目標相同。
4. `osrm.route(...)` 算路線；**步行 > 600 秒 → 無效**。
5. 都過 → `_commit_normal` 結算。

**`_commit_normal`（buffer 翻面規則）：**

```python
new_state = deepcopy(state)        # 先「整份複製」一份再改
new_target.owner = cur             # 1. 目標一定翻成自己的
# 2. 用 geometry 算 50m 走廊，找出落在走廊裡的 POI（起點/目標本身不算）
# 3. 阻斷規則：數走廊內有幾個中立點，達到門檻才算被擋
neutral_count = 0
for poi in in_buffer:
    if poi.owner is None:
        neutral_count += 1
blocked = neutral_count >= self.block_neutral_count   # 門檻是設定值，預設 2
if not blocked:
    for poi in in_buffer:
        if poi.owner == 對手:       # 沒被擋 → 走廊內對手全翻
            poi.owner = cur
```

> `block_neutral_count` 是 `app/config.py` 的 `GAME_BLOCK_NEUTRAL_COUNT`（預設 2）。
> 設 1＝原本「有任何中立點就被擋」；設越大＝越難被擋＝越容易一路翻對手（拉扯感旋鈕）。

- `deepcopy(state)`：**整份複製**再改。萬一要取消，原本的 state 完全沒被動到。**這保證「無效的一手是完全的 no-op」。**
- 結算完 `turn_index += 1`、`_maybe_finish` 檢查是否該結束。

**`_maybe_finish`（三個結束條件）：** 回合到上限 / 沒中立 POI / 連續兩 pass → `status = "finished"`。

### 4-9. `app/map/render.py` — 用 Folium 畫地圖（普通函式）

`render_map_html(state)`：算中心 → `folium.Map(...)` → 畫最新一條路線 `folium.PolyLine` + 走廊 `folium.Polygon` →
for 迴圈每個 POI 畫 `folium.CircleMarker`，依「階段+屬於誰」給不同 popup → 最後塞一段 JavaScript。

> 要會解釋的點：
> - **座標對調**：Folium 要 `[緯度, 經度]`，OSRM 給 `[經度, 緯度]`。
> - **popup 裡不能放 `<script>`**：會把 Folium 自己的程式區塊提前關掉。互動 JS 統一放最後的 `_page_script`。
> - 這裡的 `poi` 是 `Poi` 物件 → 用 `poi.name`；`route` 是字典 → 用 `route["..."]`。

### 4-10. `app/web.py` — Flask 網站（把一切接起來）

開頭先**建立要用的物件**（共用）：

```python
app = Flask(__name__)
store = StateStore(...);  osrm = OsrmClient();  nominatim = NominatimClient()
overpass = OverpassClient(...);  engine = RulesEngine()
```

再用 decorator 把函式綁到網址：

```python
@app.route("/move", methods=["POST"])   # 有人 POST /move 就執行下面
def move():
    state = store.load()
    result = engine.apply_move(state, target_poi_id, osrm, source_poi_id=source_poi_id)
    if result["ok"]:
        store.save(result["state"])      # 合法才存檔
    ...
    return redirect(url_for("index"))    # 轉回首頁
```

每個路由都很單純：讀存檔 → 呼叫對應物件的方法 → （合法才）存檔 → `flash` 訊息 → 轉回首頁。
`flash` = 在下一頁頂端顯示提示；`redirect` = 叫瀏覽器跳到某頁（每次都轉址，重新整理才不會重送表單）。

### 4-11. 前端：`templates/*.html` + `static/app.css`

- `index.html`：主畫面（左欄 + 右邊地圖 iframe），用 Jinja 模板 `{{ 變數 }}`、`{% if %}` 把 Python 傳進來的值填進網頁。
- `setup.html`：開局的「搜尋起始地點」畫面。
- `app.css`：純樣式。
- 版面/操作（UI/UX）跟原本一樣；只有「遊戲規則說明」那段文字改成反映新的阻斷門檻（會顯示「要幾個中立點才擋」）。
  （`{{ state.turn_index }}` 這種寫法，對 `GameState` 物件就是取它的 `turn_index` 欄位。）

---

## 5. 教授可能會問，你可以這樣答

**Q：你哪裡用到了 class / OOP？**
A：核心都用 class：`Poi`、`GameState`（資料+方法）、`StateStore`（存檔）、三個 API 服務（`OsrmClient` 等）、
`RulesEngine`（規則）。每個都是 `__init__` 裝好資料、再提供方法操作，把「資料和操作」綁在一起。

**Q：為什麼 `RulesEngine.apply_move` 要把 `osrm` 當參數傳進去，而不是自己 import？**
A：這樣正式跑可以傳真的 `OsrmClient`，測試可以傳假的 `FakeOsrm`（回傳固定路線、不連網），引擎本身完全不用改。
規則檢查 `check_rules.py` 就是這樣離線測的。

**Q：你的資料怎麼存？**
A：整場遊戲是一個 `GameState` 物件。要存檔時用 `to_dict()` 把它（含每個 `Poi`）變成字典，再 `json.dump` 寫成 `data/state.json`；
讀檔時 `json.load` 變字典、再 `from_dict()` 做回物件。每次請求都「讀→算→寫」，沒用資料庫，因為單機一局一個檔案就夠。

**Q：「50 公尺 buffer」怎麼算？為什麼要轉座標？**
A：度不能直接當公尺，先用 pyproj 轉成公尺，再用 shapely 的 `.buffer(50)` 把路線加寬成走廊，
最後 `.covers()` 判斷每個 POI 在不在走廊裡。在 `app/services/geometry.py`。

**Q：翻面規則？**
A：目標一定翻成我的；再看路線兩側 50m 走廊裡有幾個**中立**點：達到門檻（`GAME_BLOCK_NEUTRAL_COUNT`，預設 2 個）就視為被擋、只翻目標；
不到門檻就把走廊裡**所有對手**翻給我。自己的點不擋路也不被翻。在 `RulesEngine._commit_normal`。
（門檻設 1＝原本「有任何中立點就擋」；調大＝翻面更常發生、更有拉扯感。）

**Q：無效的一手會不會留下髒資料？**
A：不會。先用沒改過的 state 做完所有檢查，全通過才 `deepcopy` 一份來改；無效就回傳原本的 state，什麼都不動。

**Q：步行時間哪來的？**
A：`OsrmClient.route()` 呼叫真實 OSRM 服務，回傳實際步行秒數，超過 600 秒那手無效。

**Q：Nominatim 跟 Overpass 差在哪？**
A：Nominatim 是「地名→座標」（開局找中心點）；Overpass 是「抓中心點附近一堆地點」組成棋盤。

**Q：為什麼用 shapely/pyproj/Overpass（課好像沒教）？**
A：50 公尺走廊要精準在地圖上畫出來、判斷點在不在裡面，用 shapely/pyproj 最準；Overpass 能一次抓附近大量地點當棋子。
它們在 `geometry.py` / `overpass.py`，每一行我都能解釋。

---

## 6. 怎麼跑起來

```bash
pip install -e .          # 安裝套件（只需一次）

python -m app.web         # 啟動，瀏覽器開 http://127.0.0.1:5000

python check_rules.py     # （可選）跑規則檢查，全部 PASS 代表規則正常
```

**`check_rules.py` 是什麼：** 精簡規則檢查（約 12 項，每項對應一條規則）。
它「不連網路」——做一個假的 OSRM 物件 `FakeOsrm`（`.route()` 回傳固定路線）傳給引擎，所以又快又穩。

> **Nominatim 回 403 Forbidden**：`User-Agent` 要能識別你的程式 + 真實信箱（假信箱被擋）。
> 改 `app/config.py` 的 `NOMINATIM_USER_AGENT` / `NOMINATIM_EMAIL`（已預設成可用值）。
>
> **SSL 憑證錯誤（學校網路常見）**：在 `app/services/` 三個有 `requests.get/post` 的地方，
> 把該行加上參數 `verify=False`（關閉憑證檢查，僅作業用）。

---

## 附：這個版本和「最原始那版」的差別

最原始的版本也用 class，但用了**超出課程**的進階寫法。我把它降到「課堂教的程度」：

| 最原始（超綱） | 改成（課堂教過） |
|---|---|
| `@dataclass`（自動生成 class） | 自己寫 `class` + `__init__`（你課堂學的寫法） |
| `Protocol` + 依賴注入抽象 | 直接「把物件當參數傳進去」（`apply_move(..., osrm)`） |
| `httpx` | `requests`（課堂教的） |
| 自訂例外類別、快取、限速、atomic 寫檔、SSL truststore | 全部拿掉，連線就 `requests` 一行、存檔就 `with open` |
| type hint（`str | None` 等） | 拿掉，函式參數寫單純 |
| 250 個 pytest（含 fixture/monkeypatch） | 一支 `check_rules.py`（純 Python、離線、約 12 項） |

**沒有改的（行為一模一樣）：** 遊戲規則、計分、結束條件、地圖長相、所有畫面與操作（HTML/CSS/前端 JS）。
