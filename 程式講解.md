# GeoFlip 程式逐檔講解

> 這份文件用來幫你「看懂每一行、能跟教授解釋」。
> 全部用你學過的觀念（變數、函式、if/for/while、list、dict）來說明；
> 真的不得不用到套件的特殊寫法時，會特別圈出來告訴你「這行是套件規定的，怎麼解釋」。

---

## 0. 這個遊戲在做什麼（30 秒版）

兩個人輪流在「真實地圖」上搶地盤：

1. 開局先在地圖上點幾個地點佈子（插旗）。
2. 之後每一回合：先點自己的一個地點當「起點」，再點一個中立地點當「目標」，
   系統用 **真實步行路線** 把兩點連起來。
3. 目標一定變成你的；如果路線兩側 50 公尺內「沒有中立點擋路」，沿途的對手地點也會被你翻過來。
4. 回合用完、或地點被佔光、或連續兩次跳過，就結束，分數高的贏。

---

## 1. 整體架構：你點一下，背後發生什麼事

這是一個「網站」程式。瀏覽器是畫面，Python 在後面算規則。**一次「連線到此」的點擊**會這樣跑：

```
瀏覽器送出表單  POST /move（夾帶 目標id、起點id）
   │
   ▼
app/web.py        ← Flask 收到請求，呼叫規則引擎
   │
   ▼
app/game/rules.py ← 判斷合不合法、算要翻誰（中間呼叫 OSRM 算路線）
   │
   ▼
app/state.py      ← 把新的遊戲狀態存回 data/state.json
   │
   ▼
轉址回首頁 GET /  →  app/web.py 再讀一次存檔，重新畫畫面
                      其中地圖是 app/map/render.py 用 Folium 畫出來的
```

**關鍵設計：程式本身不「記住」任何東西。** 每次請求都是「讀檔 → 算 → 寫檔」。
所以你重新整理瀏覽器也不會出錯——因為真相永遠在 `data/state.json` 這個檔案裡。

---

## 2. 五個外部服務／套件，各是什麼？（作業要求的重點）

| 名稱 | 是什麼 | 在這專案做什麼 | 在哪個檔案 |
|---|---|---|---|
| **Flask** | 一個「做網站」的套件 | 接住瀏覽器的請求、回傳網頁 | `app/web.py` |
| **Nominatim** | 「地名 → 座標」的搜尋服務 | 開局時你打「新竹車站」，它告訴你那在哪 | `app/services/nominatim.py` |
| **Overpass** | 「抓某區域一堆地點」的服務 | 以中心點抓附近 18~36 個 POI 組成棋盤 | `app/services/overpass.py` |
| **OSRM** | 「算路線」的服務 | 算兩點之間的步行路線與時間（規則的 600 秒上限靠它） | `app/services/osrm.py` |
| **Folium** | 「用 Python 畫互動地圖」的套件 | 把 POI、路線畫成可點擊的網頁地圖 | `app/map/render.py` |
| **Shapely + pyproj** | 幾何/座標計算套件 | 把路線「加寬成 50 公尺走廊」並判斷哪些點落在裡面 | `app/services/geometry.py` |

> 這三個服務（Nominatim / Overpass / OSRM）都是 **網路上的免費 API**：
> 我們把參數組成一個網址用 `httpx` 送出去，它回我們一包 JSON（就是巢狀的字典/串列），我們再讀出需要的欄位。

---

## 3. 資料長什麼樣：`state` 字典

整場遊戲只有一份資料，叫 **state**，它就是一個**大字典**。看懂它，全部程式就懂一半了：

```python
state = {
    "game_id": "game_xxxx",   # 這一局的編號
    "turn_index": 0,          # 現在第幾回合（從 0 算；偶數輪 P1、奇數輪 P2）
    "max_turns": 20,          # 總共幾回合
    "players": {"1": {...}, "2": {...}},
    "pois":   [ {POI字典}, {POI字典}, ... ],   # 棋盤上所有地點
    "routes": [ {路線字典}, ... ],             # 畫過的路線
    "moves":  [ {一手紀錄}, ... ],             # 每一手的歷史
    "status": "active",       # active=進行中 / finished=結束
}
```

一個 **POI（地點）** 也是字典：

```python
{
  "id": "node:123",          # 唯一編號
  "name": "新竹車站",
  "lat": 24.80, "lon": 120.97,
  "category": "railway", "poi_type": "station",  # 種類
  "score": 2,                # 佔領可得幾分
  "owner": None,             # 屬於誰：None=中立、1=P1、2=P2  ← 整個遊戲就在改這個欄位！
  ...
}
```

> **一句話講完整個遊戲的本質：** 從頭到尾就是在改每個 POI 的 `owner`，再把分數加總。

---

## 4. 逐檔講解

檔案我「由底層往上」排，因為上層會用到下層。

### 4-1. `app/config.py` — 所有設定值

就是一堆**普通變數**，例如 `GAME_MAX_WALK_SECONDS = 600`。
想改遊戲行為（回合數、走廊寬度…）就改這裡的數字。

- 原版用了 class 跟「環境變數」讀法，我**全部拿掉**，因為我們不需要「從外面改設定」。
- 怎麼跟教授說：「設定我集中放成一個檔案的常數，要調參數只改一個地方。」

### 4-2. `app/models.py` — 資料 + 對資料的小計算

這裡只有**普通函式**，分三種：

1. `score_poi(category, poi_type)`：一串 if，依種類回傳 1/2/3 分。
2. **建立資料的函式**：
   - `make_poi(...)`：組出一個 POI 字典（`owner` 預設 None）。
   - `new_game(...)`：回傳一場全新的空白 `state` 字典。
3. **讀 state 的小工具**（全都是「給 state、回傳算好的答案」）：
   - `current_player_id(state)`：`turn_index % 2` 偶數→1、奇數→2。
   - `get_poi(state, id)`：用 for 迴圈找出某個 POI。
   - `scores(state)`：把每個 POI 的分數依 `owner` 加總。← **規則第 9 條：分數即時算，不存舊值。**
   - `neutral_pois` / `owned_pois` / `is_finished` / `in_opening_phase` …

> 用到的特殊東西只有兩個，都很好解釋：
> - `import uuid`：產生不重複的編號（`uuid.uuid4().hex`）。
> - `from datetime import datetime`：取現在時間存進存檔。

### 4-3. `app/state.py` — 存檔 / 讀檔

三個函式，這就是你「沒學過的檔案讀寫」，但其實只有兩種寫法：

```python
with open(path, "r", encoding="utf-8") as f:   # 讀
    return json.load(f)        # 把 JSON 文字 → Python 字典

with open(path, "w", encoding="utf-8") as f:   # 寫
    json.dump(state, f, ensure_ascii=False, indent=2)  # 字典 → JSON 文字
```

- `with open(...) as f:` = 「開檔，用完自動關檔」。
- `json.load` / `json.dump` = 字典和「JSON 文字」互轉。JSON 長得跟 Python 字典幾乎一樣。
- 原版用「先寫暫存檔再換掉」防當機，我改成直接寫，比較好懂（這就是你說可以犧牲的部分）。

### 4-4. `app/services/geometry.py` — Shapely + pyproj（作業重點之一）

**要解決的問題：** 地圖座標是「經緯度（度）」，不能直接量公尺。所以：

```
經緯度 ──(pyproj 換算)──► 公尺座標 ──(shapely 加寬/判斷)──► 答案
```

五個函式：

- `choose_metric_crs(lon, lat)`：依經度算出該用哪個「公尺座標系(UTM)」。
- `build_meter_transformers(...)`：做出兩個轉換器（經緯度↔公尺）。
  - **`always_xy=True` 一定要加**：保證順序都是 (經度, 緯度)。
- `route_to_meter_linestring(coords, to_meters)`：把路線上每個點換成公尺，組成一條線 `LineString`。
- `buffer_route_meters(line, 50)`：`line.buffer(50)` 把線往兩側加寬 50 公尺，變成一個多邊形（走廊）。← **這就是「50 公尺 buffer」。**
- `point_in_buffer(...)`：`polygon.covers(Point(x, y))` 判斷某點在不在走廊裡（含邊界）。

> 特殊寫法只有「呼叫套件」：`Transformer.from_crs(...)`、`line.buffer(...)`、`polygon.covers(...)`。
> 怎麼解釋：「這幾行是直接用 pyproj / shapely 提供的功能，分別做座標轉換、把線加寬、判斷點在不在多邊形裡。」

### 4-5. `app/services/osrm.py` — 算步行路線

一支函式 `osrm_route(起點緯, 起點經, 終點緯, 終點經)`：

1. 把座標組成一個**網址**（OSRM 規定座標順序是「經度,緯度」）。
2. `httpx.get(url, ...)` 送出請求，`resp.json()` 把回應轉成字典。
3. 從字典裡讀出 `coordinates`（路線座標）、`distance`（距離）、`duration`（秒數），打包成字典回傳。
4. 出任何錯（沒網路、找不到路）就 `raise Exception`，呼叫它的規則引擎會把這手當無效。

> `try: ... except Exception as exc: ...` = 「試著做；如果出錯，就改做這個」。
> 規則「步行超過 600 秒無效」就是靠這支函式回傳的 `duration_s`。

### 4-6. `app/services/nominatim.py` — 地名搜尋（開局選地點）

一支函式 `search_locations(query)`：把關鍵字組成網址送給 Nominatim，
回傳一個**候選清單**，清單裡每個元素是字典（含 `display_name`、`lat`、`lon` 等）。

- 用 for 迴圈一筆筆整理結果，順手**去掉重複**、把**台灣的結果排前面**（用兩個串列分裝再相加，沒有用 lambda）。
- 原版的快取、限速、那個 `LocationCandidate` 類別都拿掉了。

### 4-7. `app/services/overpass.py` — 抓棋盤 POI

一支主函式 `fetch_board_pois(中心緯, 中心經, 半徑, 最小間距, 上限)`：

1. `_build_query(...)`：依照 `_TAG_PROFILE`（我們接受的種類清單）組出 Overpass 的查詢字串。
2. `httpx.post(...)` 送出，拿回一包 JSON。
3. for 迴圈把每筆資料用 `_normalize(...)` 整理成 POI 字典（去重複）。
4. `sorted(..., key=_sort_key)`：分數高的排前面。
5. `_spread_filter(...)`：把「離已選的點太近」的去掉，讓棋盤分散。
6. 最多留 `limit` 個。

裡面的 `_pick_category` / `_pick_name` / `_build_raw` 都是「看 tag 決定種類、名字」的小函式。
`_CN_TYPE_NAMES` 是一張「英文種類→中文名稱」對照表（沒名字的地點才用得到）。

> `_too_close` 裡用了 `math.sqrt`、`math.cos`、`math.radians`：把經緯度差換算成大概的公尺差來量距離。

### 4-8. `app/game/rules.py` — 規則引擎（整個專案的心臟）

這是**最重要、教授最可能拷問**的檔案。全部是函式，沒有 class。

**兩個入口：**

- `apply_move(state, target_id, source_id=None)`：玩家落子。
- `apply_pass(state)`：跳過一回合。

兩者都回傳一個**結果字典**：`{"ok": True/False, "message":..., "state": 新狀態, "flipped_poi_ids":[...], ...}`。

**`apply_move` 的流程（對照規則念）：**

1. 先做檢查（**這階段絕對不改 state**）：遊戲結束了嗎？目標存在嗎？目標是中立的嗎？
2. 還在**開局階段** → 不準帶起點 → 呼叫 `_commit_opening`（只把目標插旗、回合+1）。
3. **正常回合** → 必須有起點，且起點要是自己的、不能跟目標相同。
4. 呼叫 `osrm_route` 算路線；**步行 > 600 秒 → 回傳無效**。
5. 都過了 → 呼叫 `_commit_normal` 真正結算。

**`_commit_normal`（buffer 翻面規則，規則第 4、5 條）：**

```python
new_state = deepcopy(state)        # ← 先「整份複製」一份再改
new_target["owner"] = cur          # 1. 目標一定翻成自己的

# 2. 用 geometry 算出 50 公尺走廊，找出落在走廊裡的 POI（起點/目標本身不算）
# 3. 阻斷規則：
blocked = False
for poi in in_buffer:
    if poi["owner"] is None:       # 走廊內有「中立」點 → 被擋住
        blocked = True
if not blocked:
    for poi in in_buffer:
        if poi["owner"] == 對手:    # 沒被擋 → 走廊內對手全翻
            poi["owner"] = cur
```

- `from copy import deepcopy`：**整份複製**一份 state 再改。
  好處：萬一中途判斷要取消，原本的 state 完全沒被動到。**這就是「無效的一手是完全的 no-op」的保證。**
- 結算完 `turn_index += 1`、更新時間、呼叫 `_maybe_finish` 檢查是否該結束。

**`_maybe_finish`（規則第 7 條，三個結束條件）：**
回合到上限 / 沒有中立 POI 了 / 連續兩次跳過 → `status = "finished"`。

### 4-9. `app/map/render.py` — 用 Folium 畫地圖

主函式 `render_map_html(state)`：回傳一整頁地圖 HTML。

1. 算地圖中心（所有 POI 的平均位置）。
2. `folium.Map(...)` 建地圖。
3. 畫**最新一條**路線：`folium.PolyLine(...)`（折線）+ `folium.Polygon(...)`（走廊）。
   - 走廊邊框是用 geometry 算出來、再用 for 迴圈把每個公尺座標點換回經緯度。
4. for 迴圈每個 POI 畫一個圓點 `folium.CircleMarker(...)`，並依「現在階段 + 這點屬於誰」給不同的 **popup（點開的小視窗）**。
5. 最後把一段 JavaScript 塞進 HTML。

> **兩個要會解釋的點：**
> - **座標要對調**：Folium 要 `[緯度, 經度]`，但 OSRM 給的是 `[經度, 緯度]`，所以畫線前要翻過來。
> - **popup 裡不能放 `<script>`**：否則會把 Folium 自己的程式區塊提前關掉，畫面會爆掉。所以互動用的 JS 統一放在最後那段 `_page_script`。
> - `.add_to(fmap)`、`folium.CircleMarker(...)` 這些是「呼叫 Folium 的功能」，照著套件用法寫。

那段 `_page_script` 是 **JavaScript（在瀏覽器跑，不是 Python）**，負責「在地圖上點自己的點→記住它是起點→通知旁邊的側欄」。
因為地圖被包在 `<iframe>` 裡，要跨框溝通，所以用 `postMessage` 跟 `localStorage`。這段維持原樣，UI 行為才不會變。

### 4-10. `app/web.py` — Flask 網站（把一切接起來）

開頭三行是 Flask 的核心：

```python
app = Flask(__name__)          # 做一個網站物件
@app.route("/")                # 「裝飾器」：把下面的函式綁到「/」這個網址
def index(): ...               # 有人連到首頁就執行這個
```

> **裝飾器 `@app.route(...)` 怎麼解釋：** 「它把下面那個函式『登記』成某個網址的處理器；
> 有人連到那個網址，Flask 就會呼叫這個函式。」你不用自己寫裝飾器，只要會用。

每個路由（網址）做的事都很單純：

| 網址 | 做什麼 |
|---|---|
| `GET /` | 讀存檔 → 算出畫面要的數字 → `render_template("index.html", ...)` |
| `POST /move` | 讀存檔 → `apply_move(...)` → 合法才存檔 → `flash` 訊息 → 轉回首頁 |
| `POST /pass` | 讀存檔 → `apply_pass(...)` → 存檔 → 轉回首頁 |
| `POST /new-game` | 刪存檔 → 轉回首頁（→ 會變回設定頁） |
| `POST /setup/search` | 呼叫 `search_locations` 顯示候選地點 |
| `POST /setup/start` | 呼叫 `fetch_board_pois` 抓 POI → 開新局 |
| `GET /map` | 回傳 `render_map_html(state)`（首頁的 iframe 載這個） |
| `GET /api/state` | 直接把 state 回成 JSON（方便檢查） |

- `flash(訊息, "success"/"error")` = 「在下一個畫面頂端顯示一則提示」。
- `redirect(url_for("index"))` = 「叫瀏覽器跳到首頁」。每次改完都轉址，重新整理才不會重複送出表單。
- 原版的 app factory、依賴注入、logging、`.env` 我都拿掉了，換成最直白的寫法。

### 4-11. 前端：`templates/*.html` + `static/app.css`

- `templates/index.html`：遊戲主畫面（左側欄 + 右邊地圖 iframe）。用 **Jinja 模板語法** `{{ 變數 }}`、`{% if %}` 把 Python 傳進來的數字填進網頁。
- `templates/setup.html`：開局的「搜尋起始地點」畫面。
- `static/app.css`：純樣式（顏色、排版）。

> 這些是 HTML/CSS，不是 Python。`{{ scores[1] }}` 就是把 Python 算好的分數印到網頁上。
> **這部分我完全沒動**，所以畫面/操作（UI/UX）跟原本一模一樣。

---

## 5. 教授可能會問，你可以這樣答

**Q：你的資料怎麼存？為什麼不用資料庫？**
A：整場遊戲是一個字典 `state`，存成一個 JSON 檔 `data/state.json`。
每次請求都「讀檔→算→寫檔」，沒有用資料庫是因為只有一局、單機輪流玩，一個檔案就夠，也最好懂。

**Q：「50 公尺 buffer」是怎麼算的？為什麼要轉座標？**
A：經緯度的「度」不能直接當公尺（不同緯度 1 度的實際距離不一樣）。
所以先用 pyproj 把座標換成公尺，再用 shapely 的 `.buffer(50)` 把路線加寬成走廊多邊形，
最後用 `.covers()` 判斷每個 POI 在不在走廊裡。程式在 `app/services/geometry.py`。

**Q：翻面規則是什麼？**
A：目標一定翻成我的。再看路線兩側 50 公尺走廊：
若走廊裡有任何**中立**點，視為被擋住，只翻目標；若沒有中立點，走廊裡**所有對手**的點都翻給我。
自己的點不會擋路也不會被翻。程式在 `app/game/rules.py` 的 `_commit_normal`。

**Q：無效的一手會不會留下髒資料？**
A：不會。我先用沒改過的 state 做完所有檢查，全部通過才 `deepcopy` 一份來改。
無效就直接回傳原本的 state，回合數、擁有者都不動。

**Q：步行時間怎麼來的？**
A：呼叫 OSRM 這個真實路線服務（`app/services/osrm.py`），它回傳實際步行秒數，超過 600 秒這手就無效。

**Q：地圖是怎麼來的？**
A：用 Folium 把 POI、路線在 Python 端畫成一頁 Leaflet 地圖 HTML（`app/map/render.py`），
首頁再用 `<iframe>` 把它嵌進來。

**Q：Nominatim 跟 Overpass 差在哪？**
A：Nominatim 是「地名→座標」（開局找一個中心點）；Overpass 是「抓那個中心點附近一堆地點」來組成棋盤。

**Q：為什麼分數不會算錯／不用更新？**
A：分數是即時算的（`scores()` 每次都重新把擁有的 POI 分數加總），不存舊值，所以翻面後分數自然就對。

---

## 6. 怎麼跑起來

```bash
# 安裝套件（只需做一次）
pip install -e .

# 啟動
python -m app.web
# 然後瀏覽器開 http://127.0.0.1:5000

# （可選）跑規則檢查，全部 PASS 代表規則正常
python check_rules.py
```

**`check_rules.py` 是什麼：** 一支精簡的規則檢查腳本（約 12 個重點檢查）。
它「不連網路」——把規則裡呼叫 OSRM 的函式換成一個假的、回傳固定路線的函式，
所以又快又穩。每條檢查對應一條遊戲規則（開局、目標必翻、走廊阻斷、翻對手、
超時無效、連兩次跳過結束、計分…），印出 PASS/FAIL。

> **若 Nominatim 搜尋回「403 Forbidden」**：Nominatim 規定 `User-Agent` 要能識別你的程式、
> 並附「真實」聯絡信箱（假信箱會被擋）。改 `app/config.py` 的 `NOMINATIM_USER_AGENT`
> / `NOMINATIM_EMAIL` 成你自己的學號/信箱即可（已預設成可用值）。
>
> **若出現「SSL 憑證錯誤」**：那是學校網路攔截造成的。
> 臨時解法：在 `app/services/` 三個有 `httpx.get/post` 的檔案，
> 把該行加上參數 `verify=False`（關閉憑證檢查，僅作業用）。平常不要這樣做。

---

## 附：我幫你改了哪些東西（和原版的差別）

| 原版用了（你沒學過） | 改成（你學過） |
|---|---|
| `@dataclass` 定義 Poi / GameState…，加大量 to_dict/from_dict | 全部用 **dict**；存檔直接 `json.dump`/`json.load` |
| `class StateStore` + 防當機 atomic write | 三支函式 `load_state` / `save_state` / `reset_state` |
| `class RulesEngine` + `Protocol` + 依賴注入 | 直接用**函式** `apply_move` / `apply_pass` |
| `class OsrmClient/NominatimClient/OverpassClient` + 快取/限速/SSL | 各一支**函式**，直接 `httpx` 連線 |
| `Config` 用 class + 環境變數 + `lambda` | `config.py` 一堆**普通常數** |
| `lambda`、`@property`、`@classmethod`、type hint | 通通改成普通函式 / 拿掉 |
| `tests/`（pytest、monkeypatch、fixture） | 已移除（規則我用一支臨時腳本逐條驗過才刪） |

**沒有改的（所以行為一模一樣）：** 遊戲規則、計分、結束條件、地圖長相、所有畫面與操作（HTML/CSS/前端 JS）。
